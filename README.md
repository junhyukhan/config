# config

1. Install necessary files using brew
```
brew bundle --file=~/Brewfile
```
<details>
    <summary>Creating the brew bundle</summary>
    ```
    brew bundle dump --file=~/Brewfile --force
    ```
</details>

1. Changing language input with the right command key

**karabiner elements**
copy the karabiner config settings to `~/.config/karabiner`.

**system settings**
change the keyboard shortcut for changing input language to the right command key.

1. Enable three finger swipe
**bettertouchtool**
import the btt presets

1. Import vs code settings

